# 安全策略

## 报告漏洞

如发现安全漏洞，请勿在公开 Issue 中提交。请通过以下方式私下报告：

1. 在 GitHub 仓库创建一个带 `security` 标签的私密 Issue，或
2. 直接发送邮件至维护者邮箱（见仓库 About 页）

请在报告中包含：
- 漏洞描述与影响范围
- 复现步骤
- 建议的修复方向（如有）

我们将在 7 天内确认收悉，并在 30 天内评估并发布修复方案。

## 已知注意事项

- **语音密钥**：百度 API 密钥存于 `backend/.env`（已被 `.gitignore` 忽略），
  切勿将密钥提交到版本控制或硬编码进代码
- **浏览器录音**：录音功能要求页面通过 `https://` 或 `localhost` 访问，
  生产部署务必启用 HTTPS
- **默认用户**：当前版本使用固定 `user_id=1`，无登录系统，
  不应用于多租户公开场景，后续将引入认证机制
